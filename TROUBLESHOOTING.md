# دليل حل المشكلات - Troubleshooting Guide

## المشكلة الحالية: خطأ في الاتصال بقاعدة البيانات

### الخطأ:
```
❌ خطأ في بدء الخادم: AccessDeniedError [SequelizeAccessDeniedError]:
Access denied for user 'root'@'localhost' (using password: YES)
```

### السبب:
MySQL غير مثبت أو غير مُعدّ بشكل صحيح على النظام.

---

## ✅ الحل: تثبيت وإعداد MySQL

### الخطوة 1: تثبيت MySQL

#### على Windows:

1. **تحميل MySQL:**
   - اذهب إلى: https://dev.mysql.com/downloads/mysql/
   - أو استخدم XAMPP/WAMP التي تحتوي على MySQL مدمج
   - رابط XAMPP: https://www.apachefriends.org/download.html

2. **تثبيت XAMPP (الأسهل للمبتدئين):**
   - قم بتحميل وتثبيت XAMPP
   - افتح XAMPP Control Panel
   - اضغط على "Start" بجانب MySQL
   - MySQL سيعمل على المنفذ 3306

3. **إعداد كلمة المرور (اختياري):**
   ```bash
   # إذا كنت تستخدم XAMPP، لا تحتاج كلمة مرور افتراضياً
   # المستخدم: root
   # كلمة المرور: (فارغة)
   ```

---

### الخطوة 2: إنشاء قاعدة البيانات

#### الطريقة 1: استخدام phpMyAdmin (الأسهل)

1. **افتح phpMyAdmin:**
   - على XAMPP: http://localhost/phpmyadmin

2. **أنشئ قاعدة البيانات:**
   - اضغط على "New" أو "جديد" في القائمة الجانبية
   - اسم قاعدة البيانات: `restaurant_ordering_db`
   - الترميز (Collation): `utf8mb4_unicode_ci`
   - اضغط "Create" أو "إنشاء"

#### الطريقة 2: استخدام ملف SQL (أسرع)

في phpMyAdmin:
1. اضغط على "Import" أو "استيراد"
2. اختر الملف: `setup-database.sql`
3. اضغط "Go" أو "تنفيذ"

#### الطريقة 3: سطر الأوامر

```bash
mysql -u root < setup-database.sql
```

---

### الخطوة 3: تحديث ملف `.env`

قم بفتح ملف [.env](./.env) وتأكد من الإعدادات التالية:

#### إذا كنت تستخدم XAMPP (بدون كلمة مرور):
```env
DB_HOST=localhost
DB_PORT=3306
DB_NAME=restaurant_ordering_db
DB_USER=root
# DB_PASSWORD= (اترك هذا السطر معلق أو احذفه)
```

#### إذا كنت تستخدم MySQL مع كلمة مرور:
```env
DB_HOST=localhost
DB_PORT=3306
DB_NAME=restaurant_ordering_db
DB_USER=root
DB_PASSWORD=كلمة_المرور_الخاصة_بك
```

---

### الخطوة 4: تشغيل المشروع (الجداول ستُنشأ تلقائياً!)

```bash
npm start
```

**ملاحظة مهمة:** تم تفعيل ميزة **Auto-Sync** في [server.js:89](server.js#L89)، لذلك ستُنشأ جميع الجداول تلقائياً عند تشغيل المشروع لأول مرة!

لن تحتاج لتشغيل migrations يدوياً، ولكن إذا أردت:
```bash
npm run migrate
```

أو للتطوير مع إعادة التشغيل التلقائي:
```bash
npm run dev
```

---

## 🔍 اختبار الاتصال بقاعدة البيانات

يمكنك اختبار الاتصال بقاعدة البيانات باستخدام هذا الأمر:

```bash
# على Windows مع XAMPP
mysql -u root -h localhost
```

إذا نجح الاتصال، ستظهر لك واجهة MySQL.

---

## 📋 قائمة التحقق السريعة

- [ ] MySQL مثبت ويعمل
- [ ] قاعدة البيانات `restaurant_ordering_db` تم إنشاؤها
- [ ] ملف `.env` مُعدّ بشكل صحيح
- [ ] Migrations تم تشغيلها بنجاح
- [ ] جميع المكتبات مثبتة (`npm install`)

---

## 🆘 مشاكل أخرى شائعة

### 1. MySQL لا يعمل على المنفذ 3306

**الحل:**
```env
# في ملف .env، غيّر المنفذ
DB_PORT=3307  # أو أي منفذ آخر يستخدمه MySQL لديك
```

### 2. خطأ: قاعدة البيانات غير موجودة

```bash
# تأكد من إنشاء قاعدة البيانات أولاً
mysql -u root -e "CREATE DATABASE restaurant_ordering_db"
```

### 3. خطأ: Migrations لا تعمل

```bash
# حاول التراجع ثم إعادة التشغيل
npm run migrate:undo
npm run migrate
```

---

## 📞 الدعم

إذا واجهت مشاكل أخرى، تحقق من:
1. MySQL يعمل: تحقق من Task Manager / XAMPP Control Panel
2. المنفذ 3306 غير محجوز من برنامج آخر
3. Firewall لا يمنع الاتصال بـ MySQL

---

**تم التحديث:** 2025-11-12
