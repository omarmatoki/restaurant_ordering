# 🔄 كيفية تحديث Postman Collection

## ❓ السؤال

**عند استيراد Collection مرة أخرى، هل يتم التحديث أم إضافة Collection جديدة؟**

**الجواب:** سيتم **إضافة Collection جديدة** 🆕

---

## ✅ 3 طرق لتحديث Collection

### **الطريقة 1: حذف واستيراد من جديد** ⭐ (الأسهل)

```
1. في Postman → Collections (في الشريط الجانبي)
2. ابحث عن: "Restaurant Ordering System API"
3. اضغط على ⋮ (ثلاث نقاط) بجانبها
4. اختر "Delete"
5. اضغط Import
6. اختر الملف: Restaurant-Ordering-System.postman_collection.json
7. Done! ✅
```

**المميزات:**
- ✅ يحدث كل شيء دفعة واحدة
- ✅ تحصل على آخر تحديث
- ❌ تفقد أي تعديلات شخصية قمت بها

---

### **الطريقة 2: إضافة Request يدوياً** 🎯 (الأسرع)

بدلاً من حذف كل شيء، أضف Request الجديد فقط:

#### خطوات الإضافة:

```
1. في Postman
2. افتح Collection: "Restaurant Ordering System API"
3. افتح Folder: "🔐 Authentication"
4. اضغط "..." (بجانب اسم الـ Folder)
5. اختر "Add Request"
6. املأ البيانات ↓
```

#### البيانات المطلوبة:

**Name:**
```
Initial Register (First Admin)
```

**Method:**
```
POST
```

**URL:**
```
{{base_url}}/api/auth/register/initial
```

**Headers:**
| Key | Value |
|-----|-------|
| Content-Type | application/json |

**Body (raw → JSON):**
```json
{
  "username": "admin",
  "email": "admin@myrestaurant.com",
  "password": "admin123",
  "restaurantName": "مطعمي",
  "restaurantAddress": "شارع الملك فهد",
  "restaurantPhone": "+966501234567"
}
```

**Tests:**
```javascript
if (pm.response.code === 201) {
    var jsonData = pm.response.json();
    pm.environment.set("admin_token", jsonData.data.token);
    pm.environment.set("user_id", jsonData.data.user.id);
    pm.environment.set("restaurant_id", jsonData.data.user.restaurantId);
    console.log('✅ Admin created successfully!');
}
```

**Description:**
```
تسجيل أول مسؤول (لا يحتاج Token) - يعمل مرة واحدة فقط
```

**المميزات:**
- ✅ أسرع
- ✅ تحتفظ بكل تعديلاتك
- ❌ يدوي (لكن مرة واحدة فقط)

---

### **الطريقة 3: استيراد Request منفصل** 📦

تم إنشاء ملف منفصل للـ Request الجديد:

```
1. في Postman
2. Import
3. اختر: Initial-Register.postman_request.json
4. سيضاف Request واحد فقط إلى Collection الموجودة
5. Done! ✅
```

**المميزات:**
- ✅ لا يحذف Collection القديمة
- ✅ يضيف Request واحد فقط
- ✅ أسهل من الطريقة اليدوية

---

## 📊 المقارنة

| الطريقة | السرعة | يحذف القديم؟ | يحتفظ بالتعديلات؟ |
|---------|--------|------------|-----------------|
| **1. حذف + استيراد** | 🟢 سريعة | ✅ نعم | ❌ لا |
| **2. إضافة يدوياً** | 🟡 متوسطة | ❌ لا | ✅ نعم |
| **3. استيراد Request** | 🟢 سريعة | ❌ لا | ✅ نعم |

---

## 💡 التوصية

### إذا كانت Collection جديدة (أول مرة):
```
✅ استخدم الطريقة 1 (حذف + استيراد)
```

### إذا عدّلت على Collection وتريد الاحتفاظ بالتعديلات:
```
✅ استخدم الطريقة 3 (استيراد Request منفصل)
أو
✅ استخدم الطريقة 2 (إضافة يدوياً)
```

### إذا تريد آخر تحديث كامل:
```
✅ استخدم الطريقة 1 (حذف + استيراد)
```

---

## 🔄 كيفية معرفة Collection محدثة؟

### افتح Collection → 🔐 Authentication

**يجب أن تجد:**
```
├── Initial Register (First Admin) ⭐ جديد!
├── Login (Admin)
├── Login (Kitchen)
├── Get My Profile
├── Change Password
├── Register New User (Admin Only)
└── Logout
```

**إذا لم تجد "Initial Register":**
- استخدم إحدى الطرق أعلاه لإضافته

---

## 🆘 مشاكل شائعة

### المشكلة 1: Collection موجودة مرتين
```
الحل: احذف القديمة واحتفظ بالجديدة
```

### المشكلة 2: Request الجديد لا يعمل
```
الحل:
1. تأكد من URL: {{base_url}}/api/auth/register/initial
2. تأكد من اختيار Environment الصحيح
3. تأكد من السيرفر يعمل: npm start
```

### المشكلة 3: Token لا يُحفظ تلقائياً
```
الحل: تأكد من إضافة Tests Script:

if (pm.response.code === 201) {
    var jsonData = pm.response.json();
    pm.environment.set("admin_token", jsonData.data.token);
}
```

---

## 📝 ملاحظات مهمة

### 1. قبل الحذف
```
✅ تأكد من تصدير (Export) Collection القديمة كنسخة احتياطية
✅ احفظ أي تعديلات مهمة قمت بها
```

### 2. بعد الإضافة
```
✅ جرّب Request الجديد
✅ تأكد من حفظ Token في Environment
✅ جرّب باقي Requests للتأكد من عملها
```

### 3. للفريق
```
✅ شارك Collection المحدثة مع الفريق
✅ أخبرهم بالـ Request الجديد
✅ شارك رابط: INITIAL-SETUP.md
```

---

## 🎯 الخلاصة

### للإضافة السريعة:
```bash
# استورد هذا الملف فقط:
Initial-Register.postman_request.json
```

### للتحديث الكامل:
```bash
# احذف Collection القديمة
# ثم استورد:
Restaurant-Ordering-System.postman_collection.json
```

### للإضافة اليدوية:
```
اتبع الخطوات في "الطريقة 2" أعلاه
```

---

## 📚 المزيد من المساعدة

- [POSTMAN-GUIDE.md](POSTMAN-GUIDE.md) - دليل شامل
- [INITIAL-SETUP.md](INITIAL-SETUP.md) - شرح Initial Register
- [AUTHENTICATION-FIX.md](AUTHENTICATION-FIX.md) - حل مشكلة التسجيل

---

**آخر تحديث:** 2025-11-13
